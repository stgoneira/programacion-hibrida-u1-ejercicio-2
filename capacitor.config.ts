import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'u1.ejercicio',
  appName: 'u1-ejercicio',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
